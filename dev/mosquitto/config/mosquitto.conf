# Local-only Mosquitto for the dev HA container. No auth, no TLS — this
# broker is only reachable from inside the docker-compose network and on
# the Mac's localhost.

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_type error
log_type warning
log_type notice
